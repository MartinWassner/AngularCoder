import { MidirectivaDirective } from './midirectiva.directive';

describe('MidirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new MidirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
